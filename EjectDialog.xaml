<Window x:Class="Simple_Backup.EjectDialog"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Ejecting Drive" Height="250" Width="500"
        Background="{DynamicResource BackgroundBrush}"
    Foreground="{DynamicResource TextBrush}"
        WindowStartupLocation="CenterOwner"
  ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="20">
        <StackPanel VerticalAlignment="Stretch">
          <TextBlock Text="Ejecting Drive" FontSize="18" FontWeight="Bold" 
        Foreground="{DynamicResource TextBrush}" Margin="0,0,0,15"/>
           
         <TextBlock x:Name="StatusText" Text="Attempting to eject drive..." 
      Foreground="{DynamicResource TextBrush}" TextWrapping="Wrap" Margin="0,0,0,20"/>
           
          <ProgressBar x:Name="ProgressBar" Height="20" Margin="0,0,0,20" IsIndeterminate="True"/>
           
   <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
           <Button x:Name="ActionButton" Content="Cancel" Width="100" Height="35" Click="ActionButton_Click"/>
  </StackPanel>
        </StackPanel>
    </Grid>
</Window>
