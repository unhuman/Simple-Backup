<Window x:Class="Simple_Backup.CancellationConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Confirm Cancellation" Height="220" Width="500"
  Background="{DynamicResource BackgroundBrush}"
        Foreground="{DynamicResource TextBrush}"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
  <Grid Margin="20">
        <StackPanel VerticalAlignment="Stretch">
     <TextBlock Text="Backup Operation In Progress" FontSize="16" FontWeight="Bold" 
        Foreground="{DynamicResource TextBrush}" Margin="0,0,0,15"/>
            <TextBlock Text="The backup operation is still in progress. Are you sure you want to cancel it?" 
         Foreground="{DynamicResource TextBrush}" TextWrapping="Wrap" FontSize="12" Margin="0,0,0,25"/>
  
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
           <Button x:Name="ContinueButton" Content="Continue" Width="120" Height="40" Margin="0,0,10,0" Click="ContinueButton_Click"/>
           <Button x:Name="CancelButton" Content="Cancel Backup" Width="120" Height="40" Click="CancelButton_Click"/>
</StackPanel>
        </StackPanel>
    </Grid>
</Window>
